SECRET_KEY=replace-with-output-of-openssl-rand-hex-32
ADMIN_EMAIL=keith@mfdynamics.ai
ADMIN_PASSWORD=replace-with-strong-password
PUBLIC_IP=34.58.90.147
MAX_CUSTOMERS=0

# Email alerts (Google Workspace SMTP relay). Configure Workspace to accept the
# Arckon VM public IP, then leave SMTP_USER and SMTP_PASS blank for relay auth.
SMTP_HOST=smtp-relay.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_FROM=support@riskraven.ai
ALERT_TO=keith@mfdynamics.ai
MONITOR_INTERVAL_H=24
