server {
    listen 80;
    server_name admin.*;

    location = /dns-inventory/analyze {
        client_max_body_size 200m;
        proxy_request_buffering off;
        proxy_pass http://sentinel-admin:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 600;
    }

    # AWS Marketplace redirects with a short-lived registration token in the
    # query string. Do not write that credential to nginx access logs.
    location = /marketplace/aws/fulfillment {
        access_log off;
        proxy_pass http://sentinel-admin:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 60;
    }

    location / {
        proxy_pass http://sentinel-admin:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Cookie $http_cookie;
        proxy_read_timeout 60;
    }
}
