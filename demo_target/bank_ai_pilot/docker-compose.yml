version: '3.8'

services:
  ai-assistant:
    image: pnc-ai-assistant:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    # No TLS configured
    # No restart policy
    # No resource limits

  db:
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5432:5432"
