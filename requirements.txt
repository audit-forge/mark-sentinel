# RiskRaven Arckon — Python dependencies
# Core engine requires Python 3.9+ stdlib only.
# pyyaml is optional but recommended for docker-compose.yml parsing.
pyyaml==6.0.2
# google-auth is required only for --mode vertex (Vertex AI Gemini).
google-auth==2.40.0
# PDF report generation
fpdf2==2.8.3
# Signed agent release manifest verification and release signing helper.
cryptography==50.0.0
# AWS Marketplace SaaS fulfillment and entitlement resolution. Credentials use
# the workload's IAM role/default credential chain, never customer API keys.
boto3>=1.34.0,<2.0.0
# Remediate GHSA-6v7p-g79w-8964 (msgpack) and CVE-2025-47273 (setuptools).
msgpack>=1.2.1
setuptools>=78.1.1
# Compiled macOS agents need an explicit CA bundle for Cloudflare HTTPS.
certifi==2026.7.22
